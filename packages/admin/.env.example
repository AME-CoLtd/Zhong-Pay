# 管理后台环境变量

# 后端 API 地址（留空则使用相对路径 /api，适合同域部署）
# Vercel 独立部署时需填写后端地址，例如：
# VITE_API_URL=https://zhongpay-api.vercel.app
# Cloudflare Pages 部署时例如：
# VITE_API_URL=https://api.yourdomain.com
VITE_API_URL=
